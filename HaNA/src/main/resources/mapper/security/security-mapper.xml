<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="security">

	<select id="loadUserByUsername" resultMap="memberMap">
		select
		    *
		from
		    member m left join personality p
		    on m.id = p.member_id
		    left join interest i
		    on p.member_id = i.member_id
		where
			id = #{username}
	</select>
	
	<resultMap type="member" id="memberMap">
		<id column="id" property="id"/>
		<result column="password" property="password"/>
		<result column="name" property="name"/>
		<result column="picture" property="picture"/>
		<result column="personal_id" property="personalId"/>
		<result column="address_first" property="addressFirst"/>
		<result column="address_second" property="addressSecond"/>
		<result column="address_third" property="addressThird"/>
		<result column="address_full" property="addressFull"/>
		<result column="enabled" property="enabled"/>
		<result column="personality" property="personality"/>
		<result column="interest" property="interest"/>
	</resultMap>
</mapper>