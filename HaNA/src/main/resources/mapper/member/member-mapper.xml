<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<insert id="memberEnroll">
		insert into member values
		(#{id}, #{password}, #{name}, #{picture}, #{personalId}, #{accountType}, #{addressFull}, #{addressAll}, #{introduce}, default, #{locationX}, #{locationY})
	</insert>


	<update id="updateMember">
	 update member set
	 name = #{name},
	 address_first =  #{addressFirst},
	 <!-- addressSecound = #{addressSecound},
	 addressThrid =  #{addressThird},
	 addressFull =  #{addressFull}, -->
	 introduce = #{introduce} 
	 where 
	 	id = #{id}	
	</update>

     <update id= "updatePersonality">
     update personality
     set
     	personality = #{personality}
     where
     	member_id = #{id}
     </update>
	
	<update id="updateInterest">
	update interest
     set
     	interest = #{interest}
     where
     	member_id = #{id}
	</update>

	<select id="selectPersonality" parameterType="string" resultType="member">
		select *
		from
			personality
		where
			member_id = #{id}
	</select>
	
	<update id="updateShopMember" parameterType="member">
		update
			member
		set
			name = #{name},
			picture = #{picture},
			introduce = #{introduce},
			address_full = #{addressFull},
			address_all = #{addressAll}
		where
			id = #{id}
	</update>
	
	<update id="updateShopInfo" parameterType="map">
		update
			shop_info
		set
			bussiness_hour_start = #{bussiness-hour-start},
			bussiness_hour_end = #{bussiness-hour-end},
			location_x = #{locationX},
			location_y = #{locationY}
		where
			id = #{id}
	</update>
	
	<select id="selectShopInfo" parameterType="string" resultType="map">
		select
			*
		from
			shop_info
		where
			id = #{id}
	</select>
	
	<insert id="insertShopInfo" parameterType="map">
		insert into
			shop_info
		values(#{id}, #{bussiness-hour-start}, #{bussiness-hour-end}, #{locationX}, #{locationY})
	</insert>

	<insert id="insertPersonality">
		insert into
			personality
		(member_id, personality)
		values (#{id}, #{personality}) 
	</insert>

	<insert id="insertInterest">
		insert into
			interest 
			(member_id, interest)
		values (#{id}, #{interest})
	</insert>

	<insert id="addFollowing" parameterType ="map">
		insert into
		follower 
		values
		(#{myId}, #{friendId})
	
	</insert>

	<select id="countFollowing" parameterType="string" resultType="_int">
		select 
			count(*)
		from
			follower
		where
			member_id = #{id}
	</select>

	<select id="countFollower" parameterType="string" resultType="_int">
		select
			count(*)
		from
			follower
		where
			following_id = #{id}
	
	</select>




	<select id="selectOneMember" parameterType="string" resultType="member">
		select
		 *
		from
			member m full outer join personality p
		on m.id = p.member_id
			full outer join interest i
		on m.id = i.member_id
		where
			id = #{id}
	</select>


	<select id="followerList" resultType="follower">
		select
			following_id
		from
			follower
		where
		member_id = #{id}
	</select>






</mapper>